services:
  - type: web
    name: rootlink
    runtime: node
    buildCommand: cd frontend && npm install && npm run build && cp -r dist ../server/public
    startCommand: node server/index.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /health
